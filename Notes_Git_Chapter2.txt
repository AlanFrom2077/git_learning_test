Notes 4/30/2026 Git

Chapter 2. Local Repositories

Repository:
    - Local repository: local repository is a repository that is stored on a computer
    - Remote repository: remote repositroy is a repository that is hosted on a hosting service

Commit:
    A commit in Git is basically one version of a project. 
    Every commit has a commit hash(commit ID), Ex. 51dc6ecb327578cca503abba4a56e8c18f3835e1
    In reality, user only need the first seven characters of a commit hash to refer a commit

Areas:
    There are four important areas to be aware of when work with git
        - Working directory: the working directory is where you make all the modifications to the content of project
        - Staging area: - the Staging area is where you can add and remove files
                        - represented by index directory inside the .git directory
        - Commit history: - the commit history is where you can think of your commits existing
                          - represented by objects directory inside the .git directory

        - Local repository(.git): - the repository that stored on your computer

the working process with git:
    do modifications and saved            add files to the staging area                   add new commit to the project
        working directory       ->    local repository(.git)/staging area      ->     local repository(.git)/commit history

Command so far:
    git init -> to initialize a git repository
    git init -b <branch_name> -> initialize a git repository and set the name for the initial branch to be <branch_name> (-b means branch)
    git status -> show the state of the working directory and the staging area
    git add <filename> -> Add one file to the staging area
    git add <filename> <filename> ... -> Add multiple files to the stageing area
    git add -A -> Add all the files in the working directory that have been edited or changed to staging area (-A means All)
    git commit -m "<message>" -> Create a new commit with a commit message (-m means message)
    git log -> show a list of commits in reverse chronological order
